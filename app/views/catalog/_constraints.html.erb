<div id="appliedParams">

	<h3 class="label">You searched for:</h3>
  
  <%= render_constraints(localized_params) %>  

  <% if params[:f] || params[:q] %> 
    <%= button_to "Save search", save_search_path(session[:history].first), :method => :put, :class=>'btn primary' %>
  <% end %>
  
		
</div>
